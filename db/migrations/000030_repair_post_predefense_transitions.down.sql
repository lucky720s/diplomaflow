-- No-op: миграция 000030 только ВОССТАНАВЛИВАЕТ недостающие переходы workflow
-- (заполняет дыры в графе). Откат означал бы удаление переходов и повторную
-- поломку прохождения этапов, причём невозможно надёжно отличить добавленные
-- этой миграцией строки от исходных. Поэтому down намеренно ничего не делает.
SELECT 1;
