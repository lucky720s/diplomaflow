CREATE TABLE departments (
                             id VARCHAR(255) PRIMARY KEY,
                             name TEXT NOT NULL,
                             university_id VARCHAR(255) NOT NULL
);
