env: "local"
grpc_port: 8087
database:
  dsn: ""
# Push (FCM). Пусто => push отключён (noop). Задаётся через env в проде.
fcm:
  server_key: ""
  endpoint: ""
# Realtime через Redis Pub/Sub. Пусто => публикация отключена (no-op).
redis_addr: ""